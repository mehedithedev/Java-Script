const numbers= [1,2,3,4,5]
const secondArray= numbers

// ... is the spread operator which spreads the array into seperate element and creates a form like the following one. 
console.log(...numbers)// Outputs each and every element of an array separately
console.log(numbers)// Outputs in default form of an array